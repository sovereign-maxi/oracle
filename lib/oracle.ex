defmodule Oracle do
  @moduledoc """
  Stateless price-feed primitives: aggregation (median / mean / VWAP),
  derived prices, technical indicators, OHLC candles, and REST +
  WebSocket source adapters.

  The library ships pure functions and behaviours; process supervision,
  persistence, and state ownership are left to the calling application.

  See the README for usage examples.
  """
end
