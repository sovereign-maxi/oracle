#!/usr/bin/env bash
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

echo "==> mix format"
mix format

echo "==> mix credo --strict"
mix credo --strict

echo "==> mix test"
mix test

echo "==> mix dialyzer"
mix dialyzer --format short

echo "All checks passed."
